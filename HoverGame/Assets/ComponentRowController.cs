using UnityEngine;

public class ComponentRowController : MonoBehaviour
{

    public GameObject[] componentPanels;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public GameObject[] GetComponentPanels()
    {
        return componentPanels;
    }
}
